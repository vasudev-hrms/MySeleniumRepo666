var config = require("./config");
var http = require("http");
var login = require("m/login");
var id= request.parameters["id"];
var password = request.parameters["password"];


return login.verifyCredentials(id,password);
